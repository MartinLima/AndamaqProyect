RewriteEngine On

# Canonicaliza URLs con 301: elimina www manteniendo dominio/subdominio, ruta y querystring.
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [R=301,L]
